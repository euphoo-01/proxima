<Styles xmlns="https://github.com/avaloniaui">
    <Style Selector="Border.proxima-goal-projection-chart-card">
        <Setter Property="Background" Value="{StaticResource ProximaBrush.GradientBlueHero}" />
        <Setter Property="BorderBrush" Value="{StaticResource ProximaBrush.AccentBlueBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{StaticResource ProximaRadius.Large}" />
        <Setter Property="Padding" Value="{StaticResource ProximaSpace.24}" />
        <Setter Property="BoxShadow" Value="{StaticResource ProximaShadow.Card}" />
    </Style>
</Styles>
